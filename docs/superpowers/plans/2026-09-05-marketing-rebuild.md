# Molar marketing rebuild

Goal: launch two complete, clearly positioned versions with the same mature identity: QA at /, general browser execution at /platform.

User decisions: direct code build, Platform / Products / Solutions / Resources navigation, generalist and integration-heavy QA, full established-product presentation, deep SEO/AEO and query-driven publishing. Existing direct-to-main and deploy authorization applies. Preserve unrelated monorepo changes.

- [x] Build static semantic homepage and /platform using shared header/footer and authored interactive illustrative journey.
- [x] Build substantive Products, Solutions, Resources, Security and Company pages. Use verified claims and explicit beta availability where needed.
- [x] Retain legal and legacy reference routes; modernize the searchable blog index and full article templates.
- [x] Publish unique canonical metadata, visible-content schema, current sitemap, robots and accurate agent-readable references.
- [ ] Deferred by user until design is finished: GA4 G-2YL3J3PX8R, deeper SEO/AEO work, RSS, query-led article publishing and visitor-generated answers.
- [x] Verify independent browser behavior on desktop/mobile, keyboard menus, demo tabs, copy actions, FAQ, conversion destinations and static link/SEO contract checks.
- [x] Stage only public assets and required Pages functions; deploy existing Cloudflare project with rollback reference. Verify exact live files and routes; commit and push marketing main and parent pointer with STATUS/evidence.

## Direction
Grounded candidates: product workbench; service-route map; engineering systems manual; operations control room; circuit schematic; release record; field report. Seed a2c6e48d assigned 3, the systems manual. The user's professional-product references bind the treatment: neutral surfaces, saturated coral primary actions, exact interface diagrams, confident Hanken Grotesk typography. Reading order moves from a customer task to its observable evidence.

The monochrome product-marketing challenger is compatible with user reference choice; discipline retained: every claim adjacent to a relevant demonstration. Provenance ribbon: keep evidence attached to transitions. Cityscape: retain large-scale tonal contrast, without literal city styling. Exposure record: compare states without relying on color. Film rail: preserve step-by-step scrubbing. Risograph: one consistent brand accent, no decorative texture.

## Direction contract
THESIS: QA that follows the whole journey; platform that turns a goal into observed browser work. Explain the mechanism before product names.
OWN-WORLD: charcoal and cool paper, coral actions, precise ruled workspaces, Hanken typography and monospace only for code/data.
STORY: understand the job, inspect an illustrative journey, choose a capability, start with a scoped project.
FIRST VIEWPORT: large left-aligned headline and actions beside a detailed interactive journey workspace; complete nav above and service context below.
FORM: engineering systems manual, grounded candidate 3, seed a2c6e48d. Signature interaction selects an action and exposes its evidence.
FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance

Research: https://www.nngroup.com/articles/homepage-design-principles/ ; https://web.dev/articles/font-best-practices ; https://developers.google.com/search/docs/fundamentals/ai-optimization-guide ; https://developers.google.com/search/docs/fundamentals/using-gen-ai-content . No ranking guarantees or mass query doorway pages.

## Finish record

2026-09-05: A fresh finish-review subagent used the supplied Impeccable degraded role contract because the harness has no named-agent-type selector. It reviewed valid native viewport captures and source. Full-page stitching was invalid and discarded, so no full-page screenshot coverage is claimed. Its initial two material findings were the incomplete 843px header and the verification eyebrow. Both were corrected in one batch. The verdict pass scored both **resolved**, disposition **ship**, limited to the two fixes and supplied recaptures. Root independently verified the resulting navigation and page behavior.

The detector ran once; the progress bar now animates transform rather than width. Geist Mono remains restricted to actual technical text; Hanken is the main face. All three shipping rasters retain their original pixels and carry embedded source provenance.

## Expanded design release

After the first deployment, the user requested typography-only branding, hover navigation, autoplay, hands-on sample inputs, additional views, more illustrated sections and deeper Platform/Solutions coverage. Added six solution pages, four platform pages and a solutions hub; the release now renders 95 HTML files and 93 canonicals. Swarm remains a preview with three clearly illustrative role views.

Root independently verified autoplay, sample checkout transitions, all-observed final checks, result switching, project-name edits persisting into the result, mobile menu expansion/Escape, and no horizontal page overflow at 390/833/1440px. Pointer hover timing is source-reviewed; the available browser tool does not expose mouse-move. Background/offscreen/reduced-motion guards are source-reviewed. No live QA run is represented by a sample demo.

A fresh finish reviewer used the Impeccable role contract because this harness has no named-agent selector. Its verdict scored the three listed findings resolved, disposition **ship**, at the scope of those findings and supplied native viewport captures. Root corrected glyph icons and selected-row stripes; the alleged missing solution header was contradicted by direct visible evidence and resolved on recapture without unnecessary code changes. No second detector was run. Shipping raster provenance scan: 3 files, 0 missing. DESIGN.md and the sidecar are refreshed from the implemented extension.

Deployment: marketing source `142d64c`, Cloudflare Pages `c2b48927` at https://c2b48927.molar-it.pages.dev, promoted to https://molar.it. All 93 canonical pages and 18 public runtime assets matched the release after narrowly identified Cloudflare email transformations, its exact managed robots prefix, and the /docs/clones trailing-slash redirect. The helper preserves the managed robots policy in the evidence instead of treating it as authored content. Live browser: correct headline/wordmark, three views, autoplay, no console errors and no overflow at the normal 833px viewport.

Existing limitations: Instant Proof proxy returns 503 with missing-configuration guidance; no live verification run claimed. Cloudflare's managed robots prefix adds restrictions that differ from the site's own crawler allow list; reconcile this in the deferred SEO/AEO pass. No subdomain/DNS changes were made.
