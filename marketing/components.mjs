export const APP = 'https://app.molar.it/dashboard/signup';
export const DOCS = 'https://docs.molar.it';
export const esc = (s='') => String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const icon = (name, cls='') => {
  const paths = {
    arrow:'<path d="M4 12h15m-6-6 6 6-6 6"/>', chevron:'<path d="m7 10 5 5 5-5"/>', check:'<path d="m5 12 4 4L19 6"/>',
    globe:'<circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3c5 5 5 13 0 18-5-5-5-13 0-18Z"/>',
    map:'<path d="m3 5 6-2 6 2 6-2v16l-6 2-6-2-6 2ZM9 3v16m6-14v16"/>',
    layers:'<path d="m12 3 10 5-10 5L2 8Zm-10 9 10 5 10-5M2 17l10 5 10-5"/>',
    shield:'<path d="m12 3 8 3v6c0 5-8 9-8 9s-8-4-8-9V6ZM8 11l3 3 5-5"/>',
    trace:'<path d="M3 12h4l3-8 4 16 3-8h4"/>', code:'<path d="m8 6-6 6 6 6m8-12 6 6-6 6m-3-16-2 20"/>',
    mail:'<rect x="3" y="5" width="18" height="14" rx="2"/><path d="m3 6 9 7 9-7"/>',
    card:'<rect x="2" y="5" width="20" height="14" rx="2"/><path d="M2 10h20M6 15h3"/>',
    lock:'<rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3m-4 5v2"/>',
    cursor:'<path d="m5 3 14 10-7 1-3 7Z"/>', bolt:'<path d="m13 2-9 12h7l-1 8 10-12h-8Z"/>',
    branch:'<circle cx="6" cy="5" r="2"/><circle cx="18" cy="5" r="2"/><circle cx="6" cy="19" r="2"/><path d="M6 7v10m0-6h6a6 6 0 0 0 6-4"/>',
    file:'<path d="M14 2H5v20h14V7Zm0 0v6h5M8 12h8m-8 4h8"/>',
    copy:'<rect x="8" y="8" width="12" height="13" rx="2"/><path d="M16 8V3H3v13h5"/>',
    search:'<circle cx="10" cy="10" r="6"/><path d="m15 15 6 6"/>', terminal:'<path d="m4 6 6 6-6 6m10 0h6"/>',
    play:'<path d="m8 4 12 8-12 8Z"/>', pause:'<path d="M8 5v14m8-14v14"/>', x:'<path d="m6 6 12 12M6 18 18 6"/>',
    menu:'<path d="M4 6h16M4 12h16M4 18h16"/>', users:'<circle cx="9" cy="7" r="3"/><path d="M3 21v-3a6 6 0 0 1 12 0v3M17 4a3 3 0 0 1 0 6m1 4c3 0 4 2 4 5v2"/>',
  };
  return `<svg class="icon ${cls}" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${paths[name]||paths.globe}</svg>`;
};
export const logo = `<span class="brand-mark" aria-hidden="true"><svg width="29" height="30" viewBox="0 0 29 30" fill="none"><path d="M3 25V5h6l5.5 11L20 5h6v20h-6V15l-5.5 10L9 15v10H3Z" fill="currentColor"/></svg></span><span>Molar<span class="brand-dot">.</span></span>`;
const menus = [
  ['Platform',[
    ['/platform','The browser execution platform','Automate, explore, extract, verify.','globe'],
    ['/','Molar for QA','Test the whole user journey.','check'],
    ['/security','Security & control','Set the boundaries for automation.','shield']
  ]],
  ['Products',[
    ['/products/cartographer','Cartographer','Explore your app. Map its flows.','map'],
    ['/products/clones','Clones','Stateful services for repeatable tests.','layers'],
    ['/products/guard','Guard','Put your critical paths under watch.','shield'],
    ['/products/trace','Trace','Follow the evidence behind a run.','trace'],
    ['/products/mender','Mender','Investigate failures. Propose a repair.','bolt']
  ]],
  ['Solutions',[
    ['/solutions/web-app-testing','Web app testing','Cover the journeys users depend on.','globe'],
    ['/solutions/integration-testing','Integration-heavy apps','Payments, auth, messaging, webhooks.','layers'],
    ['/solutions/ai-agents','Coding agents','Give your agent a verification loop.','code'],
    ['/solutions/startups','Startups & small teams','Keep shipping with repeatable QA.','bolt']
  ]],
  ['Resources',[
    [DOCS,'Documentation','Get from setup to your first run.','file'],
    ['/blog','Guides & field notes','Practical lessons in software QA.','file'],
    ['/resources','Developer resources','CLI, API, MCP, and Playwright.','terminal'],
    ['/company','Company','Why we are building Molar.','users'],
    ['/changelog','Changelog','See what is new in Molar.','branch'],
    ['/contact','Talk to the team','Find the right setup for your app.','mail']
  ]]
];
const menuFeatures = {
  Platform: ['One browser. A world of possibilities.', 'Test your app, automate a workflow, or give your coding agent a way to use the web.', '/platform', 'Explore the platform', 'globe'],
  Products: ['From a user journey to an answer.', 'Explore the app. Control its dependencies. Test the release. Follow the evidence.', '/#products', 'Meet the Molar products', 'map'],
  Solutions: ['Start where your users do.', 'A sign-in. A payment. An invitation. Test the moments that make your product work.', '/solutions/integration-testing', 'Explore integration testing', 'layers'],
  Resources: ['Your next good starting point.', 'Practical guides, working commands, and a team that knows your integration problems.', '/docs', 'Open the quickstart', 'terminal']
};
export function header(active='') {
  return `<a class="skip-link" href="#main">Skip to content</a><header class="site-header"><div class="header-inner"><a class="brand" href="/" aria-label="Molar home">${logo}</a><button class="mobile-toggle" aria-label="Open navigation" aria-expanded="false" aria-controls="main-nav">${icon('menu')}</button><nav id="main-nav" class="main-nav" aria-label="Main navigation">${menus.map(([label,items])=>{
    const [title,description,href,link,i]=menuFeatures[label];
    return `<details class="nav-group"><summary>${label}${icon('chevron')}</summary><div class="nav-dropdown"><div class="dropdown-inner"><div class="dropdown-feature"><span class="menu-feature-icon">${icon(i)}</span><p>${title}</p><span>${description}</span><a href="${href}" class="text-link">${link} ${icon('arrow')}</a></div><div class="dropdown-links" aria-label="${label}">${items.map(([href,title,desc,i])=>`<a href="${href}"${href===active?' aria-current="page"':''}><span class="nav-icon">${icon(i)}</span><span><strong>${title}${title==='Mender'?'<small class="preview-label">Preview</small>':''}</strong><span>${desc}</span></span>${icon('arrow','nav-arrow')}</a>`).join('')}</div><div class="dropdown-bottom"><span>Molar works with your team and your coding agent.</span><a href="/verify">Try a browser check ${icon('arrow')}</a><a href="/contact">Talk to us ${icon('arrow')}</a></div></div></div></details>`;
  }).join('')}<div class="mobile-auth"><a href="https://app.molar.it/dashboard/login">Sign in</a><a class="button button-primary" href="${APP}">Get started ${icon('arrow')}</a></div></nav><div class="header-actions"><a class="sign-in" href="https://app.molar.it/dashboard/login">Sign in</a><a class="button button-small button-primary" data-track="signup-nav" href="${APP}">Get started ${icon('arrow')}</a></div></div></header><div class="nav-scrim" aria-hidden="true"></div>`;
}
export function footer(){return `<footer class="site-footer"><div class="wrap footer-grid"><div class="footer-brand"><a class="brand" href="/" aria-label="Molar home">${logo}</a><p>Software works in journeys.<br>Test it that way.</p><a class="text-link" href="mailto:pratik@molar.it">Talk to the team ${icon('arrow')}</a></div>${menus.map(([title,items])=>`<div class="footer-column"><h2>${title}</h2>${items.map(([href,t])=>`<a href="${href}">${t}</a>`).join('')}</div>`).join('')}</div><div class="wrap footer-bottom"><span>© ${new Date().getFullYear()} Molar Labs</span><span>Built for the web. And what comes next.</span><div><a href="/privacy">Privacy</a><a href="/terms">Terms</a><a href="/security">Security</a><a href="/pricing">Pricing</a></div></div><div class="footer-wordmark" aria-hidden="true">Molar.</div></footer>`}
export function cta(title='Ship something you have actually tested.',description='Start with one URL, one important journey, and a clear idea of what should happen.'){return `<section class="closing"><div class="wrap closing-inner"><h2>${title}</h2><div><p>${description}</p><div class="actions"><a class="button button-primary" href="${APP}" data-track="signup-footer">Get started ${icon('arrow')}</a><a class="button button-outline" href="mailto:pratik@molar.it">Talk to the team ${icon('arrow')}</a></div></div></div></section>`}
export function faq(items){return `<section class="section faq-section"><div class="wrap faq-layout"><div><h2>A few things<br>you might be wondering.</h2><p>Have a specific workflow in mind?<br><a class="text-link" href="mailto:pratik@molar.it">Let’s talk it through ${icon('arrow')}</a></p></div><div class="faq-list">${items.map(({q,a})=>`<details class="faq"><summary>${q}<span class="faq-plus" aria-hidden="true"></span></summary><p>${a}</p></details>`).join('')}</div></div></section>`}
export const faqsQA = [
  {q:'What does Molar test?',a:'Molar tests web application journeys in a real browser: navigation, forms, authenticated flows, checkout, and the outcomes users expect. For apps with external dependencies, stateful Clones provide controlled payment, email, SMS, auth, and storage behavior.'},
  {q:'Is Molar only for apps with lots of integrations?',a:'Molar is a general-purpose web QA platform. You can start with a simple form or navigation flow. Clones add a deeper testing environment when your journey crosses third-party services.'},
  {q:'How are Clones different from API mocks?',a:'A mock usually returns a prepared response. A stateful clone keeps records, advances time, and produces events for supported service behavior. That lets the next step in a journey depend on what actually happened in the previous one.'},
  {q:'Can I keep using Playwright?',a:'Yes. Playwright is part of the workflow. Use readable, editable tests and your existing engineering practices. Generated test export is available in supported workflows; confirm the hosted beta coverage for your setup.'},
  {q:'Can my coding agent use Molar?',a:'Yes. Molar exposes CLI commands and MCP tools for browser tasks, verification, extraction, and trace inspection. Agents use scoped access to authorized targets and receive structured results.'},
  {q:'How do I get started?',a:'Create an account, set up a project, and choose a URL and a bounded goal. Molar is currently in beta. For integration-heavy or hosted workflows, contact the team to confirm support and setup for your application.'}
];
export function codeBlock(code,label='Terminal'){return `<div class="code-window"><div class="code-top"><span>${icon('terminal')}${label}</span><button class="copy-button" data-copy aria-label="Copy command">${icon('copy')}<span>Copy</span></button></div><pre><code>${esc(code)}</code></pre><span class="copy-status sr-only" role="status"></span></div>`}
export function demo(kind='qa'){
 const platform=kind==='platform';
 return `<div id="interactive-demo" class="journey-demo" data-demo="${platform?'platform':'qa'}"><div class="demo-titlebar"><span class="demo-brand">${icon('trace')} Molar <span class="demo-slash">/</span> <span>${platform?'Browser workspace':'Browser QA'}</span></span><span class="demo-caption">Illustrative demo</span></div><div class="demo-tabs" role="tablist" aria-label="${platform?'Browser task examples':'Test journey examples'}">${(platform?['Automate','Explore','Extract']:['Checkout','Signup & verify','Core app']).map((t,i)=>`<button role="tab" id="demo-tab-${i}" aria-selected="${i===0}" tabindex="${i===0?0:-1}" aria-controls="demo-panel" data-demo-tab="${i}">${t}</button>`).join('')}</div><div id="demo-panel" role="tabpanel" aria-labelledby="demo-tab-0"><div class="demo-goal"><span class="goal-icon">${icon('cursor')}</span><div><small>Your goal</small><p data-demo-goal>${platform?'Find the invoice and download a copy.':'Buy a plan. Check the payment and receipt.'}</p></div></div>${platform?'':`<div class="demo-conditions" data-demo-conditions><span>Test scenario</span><div class="demo-condition-options" role="group" aria-label="Checkout test scenario"><button data-condition="healthy" aria-pressed="true">Healthy checkout</button><button data-condition="failure" aria-pressed="false">Missing webhook</button></div></div>`}<div class="demo-browser-bar">${icon('globe')}<span data-demo-url>${platform?'demo.acme.test/billing':'demo.acme.test/checkout'}</span><span class="sandbox-label">Test environment</span></div><div class="demo-flow"><div class="flow-line" aria-hidden="true"></div>${(platform?['Open billing','Find invoice','Download file']:['Open checkout','Confirm payment','Receive receipt']).map((t,i)=>`<button class="flow-step ${i===0?'is-selected is-complete':''}" data-step="${i}" aria-pressed="${i===0}"><span class="step-icon">${icon(['globe','card','mail'][i])}</span><span data-step-name>${t}</span><span class="step-check">${icon('check')}</span></button>`).join('')}</div><div class="demo-inspector"><div class="inspector-heading"><span data-inspector-title>${platform?'Billing is open':'Checkout is ready'}</span><span class="status-pass">${icon('check')}<span>Observed</span></span></div><div class="checkout-surface" data-inspector-surface>${platform?`<div class="demo-result">${icon('file')}<strong>Acme billing</strong><span>Invoices and payment history.</span><code>Authorized workspace</code></div>`:`<div class="sample-plan"><span class="sample-avatar">A</span><div><strong>Acme workspace</strong><small>Pro plan · Test purchase</small></div><span class="sample-plan-price">$24<span>/mo</span></span></div><div class="sample-field"><span>Card details</span><div>${icon('card')}<span>4242&nbsp; 4242&nbsp; 4242&nbsp; 4242</span><span>Test card</span></div></div><div class="sample-order-footer"><span>${icon('lock')} Stripe clone</span><span>Ready to confirm ${icon('arrow')}</span></div>`}</div><div class="observed-line">${icon('check')}<span data-inspector-evidence>${platform?'Authenticated billing page visible.':'Payment form visible. Test service connected.'}</span></div></div><div class="demo-action">${icon('cursor')}<span data-demo-action>${platform?'Open the billing page.':'Fill the checkout with a test card.'}</span></div><div class="demo-player"><button class="demo-play" data-demo-play aria-label="Play demo">${icon('play')}<span>Play demo</span></button><div class="demo-progress" aria-hidden="true"><span data-demo-progress></span></div><span class="demo-progress-label" data-demo-progress-label>Step 1 of 3</span></div><div class="demo-bottom"><span><i></i> Isolated test data</span><span>Sample walkthrough · No live run</span></div><span class="sr-only" role="status" data-demo-status></span></div></div>`;
}
export function visual(kind='explore'){
 if(kind==='clones')return `<div class="product-visual clones-visual"><div class="visual-top">${icon('layers')}<span>Connected test services</span><span class="demo-caption">Illustrative setup</span></div><div class="clone-world"><div class="world-app">${icon('globe')}<strong>Your application</strong><span>One isolated test environment</span></div><div class="world-connector" aria-hidden="true"></div><div class="service-rack">${[['S','Stripe','Payment state'],['@','Email','Verification & receipts'],['T','Twilio','SMS & OTP'],['A','Auth','Sessions & identities'],['S3','Storage','Files & uploads']].map(([m,n,d])=>`<div><span class="provider-mark">${m}</span><strong>${n}</strong><span>${d}</span>${icon('check')}</div>`).join('')}</div></div><div class="visual-foot"><span>Snapshot</span><span>Advance time</span><span>Restore</span></div></div>`;
 if(kind==='guard')return `<div class="product-visual guard-visual"><div class="visual-top">${icon('branch')}<span>Release checks</span><span class="demo-caption">Illustrative run</span></div><div class="pr-title"><span class="pr-branch">${icon('branch')}<span>Update checkout confirmation</span></span><span>Pull request #48</span></div><div class="check-list">${[['check','Signup and verification','Passed','pass'],['check','Invite a teammate','Passed','pass'],['x','Payment confirmation','Failed','fail']].map(([i,n,s,c])=>`<div><span class="check-symbol ${c}">${icon(i)}</span><strong>${n}</strong><span class="${c}">${s}</span></div>`).join('')}</div><div class="failure-note"><strong>The payment succeeded. The order did not update.</strong><p>Expected “Confirmed” after the payment callback.<br>The order remains “Pending”.</p><span>Open the trace ${icon('arrow')}</span></div><div class="visual-foot"><span>Preview environment</span><span>3 journeys checked</span></div></div>`;
 if(kind==='trace')return `<div class="product-visual trace-visual"><div class="visual-top">${icon('trace')}<span>Checkout / evidence</span><span class="demo-caption">Illustrative trace</span></div><div class="trace-comparison"><div><small>Expected outcome</small><div class="mini-receipt">${icon('check')}<strong>Order confirmed</strong><span>Receipt sent to test inbox</span></div></div><div><small>Observed outcome</small><div class="mini-receipt fail-receipt">${icon('x')}<strong>Order pending</strong><span>Payment callback not applied</span></div></div></div><div class="trace-events"><div><span class="event-time">00:08</span><span>Payment confirmed</span><span class="pass">200</span></div><div><span class="event-time">00:09</span><span>POST /webhooks/payment</span><span class="fail">500</span></div><div><span class="event-time">00:10</span><span>Order status assertion</span><span class="fail">Failed</span></div></div><div class="visual-foot"><span>Screenshot</span><span>Network</span><span>Console</span><span>Steps</span></div></div>`;
 if(kind==='agent')return `<div class="product-visual agent-visual"><div class="visual-top">${icon('code')}<span>Code → browser → evidence</span><span class="demo-caption">Example workflow</span></div><div class="agent-message"><span class="agent-avatar">${icon('code')}</span><div><strong>Coding agent</strong><p>The login form is implemented.<br>Check that the dashboard loads after sign-in.</p></div></div><div class="agent-command"><span>molar verify</span> "login works"<br><span class="code-indent">--url http://localhost:3000 --json</span></div><div class="agent-message molar-message"><span class="agent-avatar">M</span><div><strong>Molar</strong><p>Open the browser. Execute the flow.<br>Return the observed outcome and trace.</p></div></div><div class="agent-loop"><span>Inspect</span>${icon('arrow')}<span>Fix</span>${icon('arrow')}<span>Verify again</span></div></div>`;
 return `<div class="product-visual map-visual"><div class="visual-top">${icon('map')}<span>Application map</span><span class="demo-caption">Illustrative flow</span></div><div class="map-root">${icon('globe')}<strong>Your application</strong></div><div class="map-branches"><div><span class="map-branch-title">Guest</span><div class="map-node">Homepage ${icon('check')}</div><div class="map-node">Browse plans ${icon('check')}</div><div class="map-node">Create account ${icon('check')}</div></div><div><span class="map-branch-title">Member</span><div class="map-node">Sign in ${icon('check')}</div><div class="map-node">Workspace ${icon('check')}</div><div class="map-node map-node-focus">Invite teammate ${icon('cursor')}</div></div></div><div class="map-legend"><span><i class="legend-observed"></i>Observed path</span><span><i class="legend-next"></i>Next branch</span></div><div class="visual-foot"><span>Pages</span><span>Actions</span><span>Outcomes</span><span>Evidence</span></div></div>`;
}
