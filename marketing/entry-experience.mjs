import {APP, icon} from './components.mjs';

export function entryNote() {
  return `<p class="entry-note">No account needed for a public-page check. <a href="/pricing">Bringing a private app? See beta setup ${icon('arrow')}</a></p>`;
}

export function firstStep(platform=false) {
  return `<section class="section first-step" id="start"><div class="wrap first-step-grid"><div><h2>${platform?'Start with one task.':'Start with one check.'}<br>See what comes back.</h2><p>Try Molar on a public page before connecting your app. Say what should appear on the page, then review whether Molar found it and the screenshot it captured.</p><a class="button button-primary" href="/verify">Try a browser check ${icon('arrow')}</a><span class="first-step-note">No signup · Read-only · Public pages</span></div><div class="first-step-path"><ol><li><span>${icon('globe')}</span><div><h3>Choose a public URL</h3><p>A page you own or have permission to check. Use the supplied example to explore first.</p></div></li><li><span>${icon('cursor')}</span><div><h3>Say what should be there</h3><p>For example: “The page has a heading called Example Domain.” Molar opens a browser and checks.</p></div></li><li><span>${icon('trace')}</span><div><h3>Review the result</h3><p>See whether the check passed and inspect the captured page. Share the result, or create an account to save it.</p></div></li></ol><div class="first-step-next"><strong>Ready for your own app?</strong><p>We’ll help set up the test account, connected services, and checks your app needs during beta.</p><div><a href="/contact">Talk through your setup ${icon('arrow')}</a><a href="${APP}">Create a workspace ${icon('arrow')}</a></div></div></div></div></section>`;
}

export function recordedEvidence() {
  return `<section class="section evidence-chapter" id="recorded-run"><div class="wrap"><div class="evidence-heading"><h2>Watch a real<br>check finish.</h2><div><p>One instruction: add an element, check it appeared, remove it, and check it is gone.</p><a class="text-link" href="/examples/autonomous-browser-check">Open the full run ${icon('arrow')}</a></div></div><div class="evidence-workbench"><div class="evidence-footage"><div class="evidence-caption"><span>${icon('play')} Actual browser recording</span><span>43 seconds · Unedited</span></div><div class="recording-player" data-recording><video controls playsinline preload="none" src="/assets/browser-check/video.webm" poster="/assets/browser-check/final.jpg" width="1280" height="720" aria-label="Molar adding and removing an element on a public test page">Your browser cannot play this video. <a href="/assets/browser-check/video.webm">Download the recording</a>.</video><button class="recording-start" data-recording-start hidden aria-label="Play the actual browser check"><img src="/assets/browser-check/final.jpg" width="1280" height="720" alt=""><span>${icon('play')} <span data-recording-label>Play the actual run</span></span></button></div></div><aside class="evidence-observations"><div class="evidence-verdict">${icon('check')} All four steps passed.</div><ol>${[['Add the element','Clicked Add Element once.'],['Check it appeared','The Delete button was present.'],['Remove the element','Clicked the Delete button.'],['Check it is gone','No Delete button remained.']].map(([title,detail])=>`<li><span>${icon('check')}</span><div><strong>${title}</strong><p>${detail}</p></div></li>`).join('')}</ol><a href="/assets/browser-check/result.json">Inspect the recorded result ${icon('arrow')}</a></aside></div><div class="evidence-context"><p>A controlled public test page, run by Molar’s browser agent. This demonstrates the recorded check; it is not a customer case study or reliability benchmark.</p><a href="/examples/team-invitation#recordings"><span>${icon('users')}</span><div><strong>See an invitation across two browsers</strong><span>Admin, test inbox, member · Controlled test</span></div>${icon('arrow')}</a></div></div></section>`;
}

export function platformFoundation() {
  return `<section class="section platform-foundation"><div class="wrap"><div class="section-heading"><h2>Get the file, the data,<br>and the run status.</h2><p>Use the returned file or data in your next tool. If the task stops, inspect the recorded steps to see where.</p></div><div class="foundation-output"><div class="foundation-browser"><span>${icon('globe')} Your web application</span><h3>Find the right page.<br>Complete the task.<br>Check the result.</h3><p>You supply the task and access.<br>Molar reads the page before acting.</p></div><div class="foundation-connector" aria-hidden="true">${icon('arrow')}</div><dl><div><dt>${icon('check')} Status</dt><dd>Completed, failed, or blocked.</dd></div><div><dt>${icon('file')} Output</dt><dd>The requested data or file, when captured.</dd></div><div><dt>${icon('trace')} Browser record</dt><dd>The steps and available screenshots from the run.</dd></div></dl></div><div class="foundation-links"><a href="/platform/browser-agents">How browser agents work ${icon('arrow')}</a><a href="/platform/live-view">Follow a run ${icon('arrow')}</a><a href="/platform/developer-tools">Connect your tools ${icon('arrow')}</a></div></div></section>`;
}
