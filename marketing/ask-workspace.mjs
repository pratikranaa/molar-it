import {icon,esc} from './components.mjs';
export function askWorkspace({prefill='',compact=false}={}){return `<div class="ask-workspace${compact?' ask-compact':''}"><form id="ask-form" action="/ask" method="post"><label for="ask-question">Your question</label><textarea id="ask-question" name="question" maxlength="400" minlength="8" rows="3" placeholder="How do I test a payment that succeeds but never unlocks the account?" required aria-describedby="ask-note">${esc(prefill)}</textarea><div class="ask-submit-row"><p id="ask-note">Use a general question. Leave out passwords, private URLs and customer details.</p><button class="button button-primary" type="submit" id="ask-submit">Get an answer ${icon('arrow')}</button><button class="button button-outline" type="button" id="ask-cancel" hidden>Cancel</button></div></form><div class="ask-suggestions" role="group" aria-label="Example questions"><span>Start with a question</span>${['How do I test delayed payment webhooks?','How can my coding agent verify a fix?','Can I test an invitation with two users?'].map(q=>`<button type="button" data-ask-question="${q}">${q}${icon('arrow')}</button>`).join('')}</div><div id="ask-progress" role="status" hidden><span class="ask-progress-line"></span><p>Reading the relevant guides…</p></div><p id="ask-error" role="alert" hidden></p><article id="ask-result" hidden><div class="ask-result-header"><h2 id="ask-result-title" tabindex="-1">Your answer</h2><button class="text-link" id="ask-save" type="button">${icon('file')} Save answer</button><button class="text-link" id="ask-copy" type="button">${icon('copy')} Copy answer</button></div><p class="ask-mode" id="ask-mode"></p><p id="ask-answered-question" class="ask-answered-question"></p><div class="ask-answer" id="ask-answer"></div><div id="ask-sources"><h3>Read the sources</h3><ul></ul></div><p class="ask-result-note">AI can make mistakes. Check the linked sources before relying on an answer.</p></article><noscript><p>Enable JavaScript to ask a question, or <a href="/blog">browse the guides</a>.</p></noscript></div>`;}
