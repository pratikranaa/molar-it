// Reviewed public facts, kept separate from private product plans and visitor input.
export const knowledge = [
 {id:'invoice-check',title:'Test an invoice download beyond the button click',url:'/blog/verify-downloaded-invoice-browser-test',terms:'download downloaded downloads downloading invoice invoices export exports file document attachment pdf csv billing',text:'For an invoice test, seed a disposable account and known invoice fields independently of the export response. Capture the download, save it before context teardown, then validate the actual document. A PDF signature or suggested filename does not establish that the invoice number, recipient, currency and total are correct. Add a parser suited to the format and assert those fields. Test unauthorized access and failed exports separately. The published Playwright example checks download completion and a PDF prefix only; it explicitly requires additional invoice-field assertions.'},
 {id:'playwright-downloads',title:'Playwright: wait for and save a download',url:'https://playwright.dev/docs/downloads',terms:'download downloaded downloads downloading invoice invoices file files export exports attachment playwright',text:'Playwright emits a download event when a page starts downloading an attachment. Begin page.waitForEvent("download") before clicking the download control, then await that promise and save the file with download.saveAs. Temporary downloads are deleted when their browser context closes, so save files that need to survive the test. Await the download in the main test flow rather than leaving an unawaited event handler.'},
 {id:'playwright-download-api',title:'Playwright: inspect download completion and filenames',url:'https://playwright.dev/docs/api/class-download',terms:'download downloaded downloads invoice invoices file filename export exports attachment playwright failure',text:'Download.saveAs copies the download to a specified path and waits for completion. Download.failure waits for completion and returns an error string or null. Download.suggestedFilename returns the browser-suggested filename, usually derived from Content-Disposition or the download attribute. Different browsers can compute it differently. A suggested filename alone does not validate the file contents. Download.path throws when connected remotely; use saveAs to copy a download to a chosen path.'},
 {id:'checkout',title:'Test a checkout beyond the payment',url:'/solutions/checkout-testing',terms:'payment payments stripe checkout billing webhook webhooks delay delayed duplicate order access refund decline',text:'A successful payment response does not prove that a customer received their order or account access. Test the browser checkout, the payment state, webhook delivery and the resulting order or entitlement separately. Exercise declined payments, a delayed notification, and retries. Molar Clones provides controlled payment state for supported Stripe behavior. A clone is not the full vendor service: finish with a small vendor-sandbox contract check for the APIs you depend on.'},
 {id:'clones',title:'Stateful services for repeatable tests',url:'/products/clones',terms:'clones clone mocks mock mocking api state stateful third party integrations dependency sandbox hermetic reset snapshot',text:'Molar Clones supplies stateful test versions of supported payment, email, SMS, identity and storage APIs. Unlike a prepared response, a stateful clone keeps records that later steps can read. Snapshot and restore make a repeat run start from a known state. Point a controlled test environment at clones; do not silently redirect production. Coverage varies by service and operation. Use vendor sandboxes to check compatibility with behavior outside the supported clone surface.'},
 {id:'email',title:'Test verification links and email codes',url:'/blog/email-workflow-testing-otp',terms:'email emails inbox mail otp code verification expired link invitation invite',text:'A useful email test checks that the message is delivered to the intended synthetic inbox, extracts the current verification link or code, opens it in the intended browser session and asserts the resulting account state. Include an expired link, a reused code and a wrong recipient. Avoid fixed sleeps: wait for the specific message. Keep tokens out of published traces and analytics. Molar supports controlled email and auth behavior through Clones; setup depends on the application.'},
 {id:'sms',title:'Test SMS and OTP flows',url:'/blog/test-twilio-sms-otp-flows',terms:'sms twilio otp phone message wrong retry code text',text:'Test SMS verification using a dedicated synthetic identity and controlled inbox. Verify the recipient, code, expiry and final authenticated state. Check a wrong code and a retry, not just message submission. Stateful service clones avoid sending real SMS during those tests. Supported Twilio behavior is scoped; keep vendor-sandbox checks for the real contract.'},
 {id:'auth',title:'Check sign-in and account access',url:'/solutions/authentication-testing',terms:'login log sign signin sign-in authentication auth authorization roles permissions password oauth onboarding account',text:'Check sign-in by asserting the protected page and expected role after authentication, rather than treating a redirect as success. Cover invalid credentials, expired sessions, sign-out and access denied for the wrong role. Use dedicated test credentials and synthetic accounts. Molar supports browser authentication workflows with configured credentials and controlled auth/email services; do not assume every identity provider or MFA flow works without setup.'},
 {id:'team',title:'Verify a team invitation across two browsers',url:'/examples/team-invitation',terms:'team teams invitation invite member admin roles users multi-user concurrent collaboration swarm',text:'The published Molar team-invitation example runs separate administrator and member browser contexts against a controlled fixture. The admin creates an invitation, the Email Clone receives one message, and the member accepts the intended invitation. The recordings are real scripted runtime evidence, not evidence of autonomous AI decisions or a concurrency benchmark. Swarm is a preview; this two-browser example does not establish large swarm capacity.'},
 {id:'guard',title:'Test a preview before merging',url:'/products/guard',terms:'guard pr pull request preview release regression merge ci github monitoring schedule deployments',text:'Guard connects browser checks to release and production workflows. Start with a small set of critical paths and explicit expected outcomes. A preview check needs the correct deployment URL, test identity and isolated service state. Review the failure evidence before making a check required for merges. Molar is in beta; product and integration availability varies. Mender is an early-access repair workflow, not a guarantee that failures are automatically fixed.'},
 {id:'trace',title:'See what happened during a run',url:'/products/trace',terms:'trace evidence debug debugger screenshots screenshot recording video replay network failure failed logs',text:'Trace connects a run result to browser steps and captured evidence. Inspect the last completed action, page state and relevant request or service event when a run fails. An individual artifact being present does not prove every requested step passed. Molar provides screenshots and run artifacts; capture availability depends on the run and environment. Treat recordings and network data as sensitive and inspect them before sharing.'},
 {id:'agent',title:'Give a coding agent a verification loop',url:'/solutions/ai-agents',terms:'agent agents coding code codex cursor mcp cli api verify verification playwright test tests',text:'A coding agent can use Molar CLI commands or MCP tools to run a browser task, inspect its result, fix a problem and verify again. A specific claim and target URL produce more useful evidence than asking whether an entire app works. Installation, authentication and a reachable target are required. Local development URLs need the configured local runtime or an authorized reachable environment. Keep conventional assertions for business invariants alongside agent exploration.'},
 {id:'platform',title:'Automate a browser task',url:'/platform',terms:'platform automation automate browser task tasks generic generalist extraction extract download form forms workflow portal',text:'The Molar platform supports browser tasks beyond QA, including navigation, forms, extracting page data and downloading files. Describe the target website, desired action and observable result. Use authorized accounts and review consequential actions. Availability and required setup vary during beta. A returned final screen is useful evidence only when it matches the requested task and its intermediate requirements.'},
 {id:'start',title:'Try a public page check or connect your app',url:'/docs',terms:'start started setup installation install onboarding configure url localhost first',text:'Try a read-only public page check at molar.it/verify without an account. Supply a URL and describe what should appear, such as a heading. For an authenticated app or browser automation, create a workspace and discuss beta setup with the team. CLI use requires installation, authentication, and a reachable test URL; the setup reference is at docs.molar.it.'},
 {id:'pricing',title:'Molar beta availability',url:'/pricing',terms:'price pricing cost free paid plan enterprise beta availability',text:'Molar is in beta. Consult the current pricing and availability page or contact the team for access and limits. This source does not establish specific prices, unlimited usage, an SLA, certification or enterprise contract terms. Do not invent them.'},
 {id:'stripe-webhooks',title:'Stripe: handle webhook events',url:'https://docs.stripe.com/webhooks',terms:'stripe webhook webhooks signature signatures duplicate duplicates retry retries order ordering delivery',text:'Stripe documents webhook signature verification, duplicate events, automatic retries and the absence of an event-ordering guarantee. A handler should verify the signature using the raw request body and endpoint secret, handle duplicates, and retrieve missing data when events arrive out of order. Check Stripe documentation for the current retry schedule and implementation examples.'},
 {id:'playwright',title:'Playwright: write resilient tests',url:'https://playwright.dev/docs/best-practices',terms:'playwright assertions locators waits waiting flaky flakiness isolated isolation test retry',text:'Playwright recommends testing user-visible behavior, isolating tests, using resilient locators and web-first assertions. Auto-waiting and retrying assertions are preferable to arbitrary timeout sleeps. Control third-party dependencies for repeatable tests. A retried pass alone does not explain why the first run failed.'}
];
